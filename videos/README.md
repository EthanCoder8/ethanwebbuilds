# Case study walkthrough videos

Drop the recordings here with these exact filenames:

- `liftnmove.mp4`  → LiftNMove case study
- `milletsfit.mp4` → MilletsFit case study

Requirements:
- Aspect ratio **16:10** (record at a 1440 x 900 browser window)
- H.264 MP4, muted, 15-25 seconds, loops seamlessly
- Target under ~4 MB each

If a file is absent the site falls back to the poster screenshot automatically.
